#include <bits/stdc++.h>
using namespace std;
void printname(int i,int n)
{
    if(i>n)
      return;
    else
      cout<<i<<"\n";
      printname(++i,n);
}

int main()
{
    int n;
    cin>>n;
    printname(1,n);

    return 0;
}
