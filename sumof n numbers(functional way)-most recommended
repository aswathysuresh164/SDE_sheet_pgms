#include <iostream>

using namespace std;
int sum(int n)
{
    if(n==0){
      
      return 0;}
    
    return n+sum(n-1);
        

      
   // sum(n-1,s+n);
}

int main()
{
    cout<<"Hello World";
    int n;
    cin>>n;
    cout<<sum(n);

    return 0;
}
