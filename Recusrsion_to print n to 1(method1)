#include <bits/stdc++.h>
using namespace std;
void printname(int n)
{
    if(n<1)
      return;
    else
      cout<<n<<"\n";
      printname(--n);
}

int main()
{
    int n;
    cin>>n;
    printname(n);

    return 0;
}
