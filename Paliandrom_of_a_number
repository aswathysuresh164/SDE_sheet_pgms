	string is_palindrome(int n)
		{
		    // Code here.
		    int d,r=0,p=n;
		    while(n!=0)
		    {
		        d=n%10;
		        r=r*10+d;
		        n=n/10;
		    }
		    if(p==r)
		       return "Yes";
		    else
		      return "No";
		    
		}
