#include <bits/stdc++.h>

using namespace std;
bool paliandrome(string s, int i)
{
    if(i==s.size()/2)
       return true;
    if(s[i]!=s[s.size()-i-1])
       return false;
    return paliandrome(s,i+1);
}

int main()
{
  
    string s="hhhh";
    cout<<paliandrome(s,0);
    
    return 0;
}
